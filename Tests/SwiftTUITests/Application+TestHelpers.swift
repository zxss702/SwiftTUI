import Foundation
@testable import SwiftTUI

extension Application {
    @MainActor
    func test_handleKeyInput(_ event: KeyEvent) {
        // Need to bypass private, or just use reflection / copy-paste
    }
}
